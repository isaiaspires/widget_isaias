import 'package:flutter/material.dart';
import 'alinhamento.dart';
import 'receita_de_bolo.dart';
import 'reuso_de_componente.dart';
import 'stone_fragments.dart';
import 'contador_de_moedas.dart';
import 'hot_reload.dart';

void main() {
  //Hot_Reload ate Gradiente
  // runApp(const App());

  //Contador de moedas
  // runApp(const Coin());

  //Reuso de Componente
  // runApp(const MyComponent());

  //Receita de Bolo
  // runApp(const Recipe());

  //Alinhamentos
  // runApp(const Alinhamento());

  //Stone Fragments
  runApp(const Stone());
}
